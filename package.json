{
  "name": "playwright-e2e",
  "version": "1.0.0",
  "main": "index.js",
  "description": "2",
  "license": "MIT",
  "scripts": {
    "test": "env-cmd -f ./.env.playwright --silent npx playwright test",
    "docker:build": "make build",
    "docker:registry": "make registry",
    "docker:push": "make push",
    "docker:run": "make run"
  },
  "dependencies": {
    "@playwright/test": "^1.19.1",
    "env-cmd": "^10.1.0",
    "typescript": "^4.5.5"
  }
}
